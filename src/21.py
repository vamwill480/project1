def add_numbers(a, b):
    """
    Add two numbers.
    
    Args:
        a (int): First number.
        b (int): Second number.
        
    Returns:
        int: Sum of a and b.
    """
    return a + b

# Example usage:
result = add_numbers(5, 3)
print(result)  # Output: 8
